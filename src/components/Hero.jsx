import React from "react";

const Hero = ({ title }) => {
  return (
    <div className="property-landing-sect-bg">
      <h3>{title}</h3>
      <div className="property-landing-dark-bg"></div>
    </div>
  );
};

export default Hero;
